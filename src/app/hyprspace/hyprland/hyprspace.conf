plugin {
    overview {
        # Layout
        hideTopLayers = false
        # 不隐藏 rofi 的层
        hideRealLayers = false

        # Styling
        dragAlpha = 0.6

        # Behaviors
        showEmptyWorkspace = false
    }
}

# `和~ 键码是 code:49，也可使用 grave
bind = ALT, grave, overview:toggle
